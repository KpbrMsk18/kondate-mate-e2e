# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: app-flows.e2e.ts >> E2E-WEB-05: 家庭の肉と魚の比率を保存できる
- Location: e2e/app-flows.e2e.ts:192:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('肉 70%', { exact: true })
Expected: visible
Error: strict mode violation: getByText('肉 70%', { exact: true }) resolved to 2 elements:
    1) <strong>肉 70%</strong> aka getByText('肉 70%').first()
    2) <strong class="ratio-meat">肉 70%</strong> aka getByRole('strong').filter({ hasText: '肉 70%' })

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText('肉 70%', { exact: true })

```

# Page snapshot

```yaml
- generic [ref=e1]:
  - banner [ref=e2]:
    - link "こんだてめいと" [ref=e3] [cursor=pointer]:
      - /url: /
      - img [ref=e5]
      - heading "こんだてめいと" [level=1] [ref=e7]
    - generic [ref=e8]:
      - navigation [ref=e9]:
        - link "献立" [ref=e10] [cursor=pointer]:
          - /url: /menu
        - link "買い物" [ref=e11] [cursor=pointer]:
          - /url: /shopping
        - link "レシピ" [ref=e12] [cursor=pointer]:
          - /url: /recipes
        - link "Pantry" [ref=e13] [cursor=pointer]:
          - /url: /pantry
        - link "ルール" [ref=e14] [cursor=pointer]:
          - /url: /rules
      - 'button "現在: システム設定に合わせる。クリックでライトモードに切り替え" [ref=e15] [cursor=pointer]':
        - img [ref=e16]
      - button "Open user menu" [ref=e19] [cursor=pointer]:
        - img "'s logo" [ref=e22]
  - main [ref=e24]:
    - generic [ref=e25]:
      - heading "家庭のルール（曜日の固定メニュー）" [level=2] [ref=e27]
      - paragraph [ref=e28]: 「金曜はカレー」のような家庭のルールです。献立生成時に AI はこのルールを破れません。同じ曜日に登録すると上書きされます。
      - generic [ref=e29]:
        - heading "肉と魚の比率" [level=3] [ref=e30]
        - paragraph [ref=e31]: 献立を自動生成するときの目安です。大豆・卵やその他の主菜も候補に含まれます。
        - generic [ref=e32]:
          - strong [ref=e33]: 肉 70%
          - slider "肉と魚の比率" [active] [ref=e34]: "70"
          - strong [ref=e35]: 魚 30%
          - button "比率を保存" [ref=e36] [cursor=pointer]
      - generic [ref=e37]:
        - heading "曜日の固定メニュー" [level=3] [ref=e38]
        - generic [ref=e39]:
          - img [ref=e40]
          - paragraph [ref=e42]: ルールはまだありません。
        - generic [ref=e43]:
          - combobox "曜日" [ref=e44] [cursor=pointer]:
            - option "月曜"
            - option "火曜"
            - option "水曜"
            - option "木曜"
            - option "金曜" [selected]
            - option "土曜"
            - option "日曜"
          - combobox "レシピ" [ref=e45] [cursor=pointer]:
            - option "レシピを選ぶ…" [selected]
          - button "ルールを登録" [disabled] [ref=e46]
        - paragraph [ref=e47]:
          - text: 先に
          - link "レシピを登録" [ref=e48] [cursor=pointer]:
            - /url: /recipes/new
          - text: してください。
  - button "Open Next.js Dev Tools" [ref=e54] [cursor=pointer]:
    - img [ref=e55]
  - alert [ref=e58]
```

# Test source

```ts
  118 |   });
  119 | 
  120 |   await page.goto("/menu");
  121 |   const monday = page.locator(".day").first();
  122 |   await monday.getByRole("button", { name: "固定", exact: true }).click();
  123 |   await expect(monday.getByText("固定", { exact: true })).toBeVisible();
  124 |   await expect(monday.getByRole("button", { name: "固定解除" })).toBeVisible();
  125 | });
  126 | 
  127 | test("E2E-WEB-04: 買い物項目を追加・編集・購入解除・削除できる", async ({
  128 |   page,
  129 | }) => {
  130 |   await prepareAuthenticatedPage(page);
  131 |   const list = {
  132 |     weekStart: "2026-07-20",
  133 |     items: [
  134 |       {
  135 |         ingredientId: "onion",
  136 |         name: "玉ねぎ",
  137 |         amount: "1個",
  138 |         purchased: false,
  139 |       },
  140 |     ],
  141 |   };
  142 | 
  143 |   await page.route("http://localhost:3001/**", async (route) => {
  144 |     const request = route.request();
  145 |     const path = new URL(request.url()).pathname;
  146 |     if (request.method() === "GET") return json(route, list);
  147 |     if (path.endsWith("/items") && request.method() === "POST") {
  148 |       const input = request.postDataJSON();
  149 |       list.items.push({ ingredientId: "carrot", ...input, purchased: false });
  150 |       await route.fulfill({ status: 204 });
  151 |       return;
  152 |     }
  153 |     const item = list.items.find(({ ingredientId }) =>
  154 |       path.includes(`/items/${ingredientId}`),
  155 |     );
  156 |     if (item === undefined) return route.fulfill({ status: 404 });
  157 |     if (path.endsWith("/unpurchase")) item.purchased = false;
  158 |     else if (path.endsWith("/purchase")) item.purchased = true;
  159 |     else if (request.method() === "PUT")
  160 |       Object.assign(item, request.postDataJSON());
  161 |     else if (request.method() === "DELETE")
  162 |       list.items.splice(list.items.indexOf(item), 1);
  163 |     await route.fulfill({ status: 204 });
  164 |   });
  165 | 
  166 |   await page.goto("/shopping");
  167 |   await page.getByLabel("追加する食材名").fill("にんじん");
  168 |   await page.getByLabel("追加する食材の分量").fill("2本");
  169 |   await page.getByRole("button", { name: "追加", exact: true }).click();
  170 |   await expect(page.getByText("にんじん")).toBeVisible();
  171 | 
  172 |   const carrot = page.locator(".shopping-item").filter({ hasText: "にんじん" });
  173 |   await carrot.getByRole("button", { name: "編集" }).click();
  174 |   await page.getByLabel("にんじんの分量").fill("3本");
  175 |   await page.getByRole("button", { name: "保存" }).click();
  176 |   const updatedCarrot = page
  177 |     .locator(".shopping-item")
  178 |     .filter({ hasText: "にんじん" });
  179 |   await expect(updatedCarrot.getByText("3本")).toBeVisible();
  180 | 
  181 |   await updatedCarrot.getByRole("checkbox").click();
  182 |   await expect(updatedCarrot.getByRole("checkbox")).toBeChecked();
  183 |   await expect(
  184 |     updatedCarrot.getByRole("button", { name: "編集" }),
  185 |   ).toBeDisabled();
  186 |   await updatedCarrot.getByRole("checkbox").click();
  187 |   await expect(updatedCarrot.getByRole("checkbox")).not.toBeChecked();
  188 |   await updatedCarrot.getByRole("button", { name: "削除" }).click();
  189 |   await expect(page.getByText("にんじん")).toHaveCount(0);
  190 | });
  191 | 
  192 | test("E2E-WEB-05: 家庭の肉と魚の比率を保存できる", async ({ page }) => {
  193 |   await prepareAuthenticatedPage(page);
  194 |   let meatRatio = 50;
  195 | 
  196 |   await page.route("http://localhost:3001/**", async (route) => {
  197 |     const request = route.request();
  198 |     const path = new URL(request.url()).pathname;
  199 | 
  200 |     if (path === "/household/fixed-menu-rules") return json(route, []);
  201 |     if (path === "/recipes") return json(route, []);
  202 |     if (path === "/household/menu-preference") {
  203 |       if (request.method() === "PUT") {
  204 |         meatRatio = request.postDataJSON().meatRatio;
  205 |         await route.fulfill({ status: 204 });
  206 |         return;
  207 |       }
  208 |       await json(route, { meatRatio });
  209 |       return;
  210 |     }
  211 |     await route.fulfill({ status: 404 });
  212 |   });
  213 | 
  214 |   await page.goto("/rules");
  215 |   const ratio = page.getByLabel("肉と魚の比率");
  216 |   await expect(ratio).toHaveValue("50");
  217 |   await ratio.fill("70");
> 218 |   await expect(page.getByText("肉 70%", { exact: true })).toBeVisible();
      |                                                          ^ Error: expect(locator).toBeVisible() failed
  219 |   await expect(page.getByText("魚 30%", { exact: true })).toBeVisible();
  220 | 
  221 |   const saved = page.waitForRequest(
  222 |     (request) =>
  223 |       request.method() === "PUT" &&
  224 |       new URL(request.url()).pathname === "/household/menu-preference",
  225 |   );
  226 |   await page.getByRole("button", { name: "比率を保存" }).click();
  227 |   expect((await saved).postDataJSON()).toEqual({ meatRatio: 70 });
  228 |   expect(meatRatio).toBe(70);
  229 | });
  230 | 
  231 | test("E2E-WEB-06: パントリーの追加・保存場所変更・削除ができる", async ({
  232 |   page,
  233 | }) => {
  234 |   await prepareAuthenticatedPage(page);
  235 |   const items = [
  236 |     {
  237 |       ingredientId: "egg",
  238 |       name: "卵",
  239 |       storageLocation: "refrigerated",
  240 |     },
  241 |   ];
  242 | 
  243 |   await page.route("http://localhost:3001/**", async (route) => {
  244 |     const request = route.request();
  245 |     const path = new URL(request.url()).pathname;
  246 | 
  247 |     if (path === "/pantry" && request.method() === "GET") {
  248 |       await json(route, items);
  249 |       return;
  250 |     }
  251 |     if (path === "/pantry/items" && request.method() === "POST") {
  252 |       const input = request.postDataJSON();
  253 |       items.push({ ingredientId: "tofu", ...input });
  254 |       await json(route, { ingredientId: "tofu" }, 201);
  255 |       return;
  256 |     }
  257 | 
  258 |     const item = items.find(({ ingredientId }) =>
  259 |       path.endsWith(`/items/${ingredientId}`),
  260 |     );
  261 |     if (item === undefined) {
  262 |       await route.fulfill({ status: 404 });
  263 |       return;
  264 |     }
  265 |     if (request.method() === "PUT") {
  266 |       item.storageLocation = request.postDataJSON().storageLocation;
  267 |     } else if (request.method() === "DELETE") {
  268 |       items.splice(items.indexOf(item), 1);
  269 |     }
  270 |     await route.fulfill({ status: 204 });
  271 |   });
  272 | 
  273 |   await page.goto("/pantry");
  274 |   await page.getByLabel("食材名").fill("豆腐");
  275 |   await page
  276 |     .getByLabel("保存場所", { exact: true })
  277 |     .selectOption("refrigerated");
  278 |   await page.getByRole("button", { name: "追加", exact: true }).click();
  279 |   await expect(page.getByText("豆腐", { exact: true })).toBeVisible();
  280 | 
  281 |   await page.getByLabel("豆腐の保存場所").selectOption("frozen");
  282 |   await expect(page.getByRole("heading", { name: "冷凍" })).toBeVisible();
  283 |   expect(
  284 |     items.find((item) => item.ingredientId === "tofu")?.storageLocation,
  285 |   ).toBe("frozen");
  286 | 
  287 |   page.once("dialog", (dialog) => dialog.accept());
  288 |   await page.getByRole("button", { name: "豆腐を削除" }).click();
  289 |   await expect(page.getByText("豆腐", { exact: true })).toHaveCount(0);
  290 |   expect(items.some((item) => item.ingredientId === "tofu")).toBe(false);
  291 | });
  292 | 
```