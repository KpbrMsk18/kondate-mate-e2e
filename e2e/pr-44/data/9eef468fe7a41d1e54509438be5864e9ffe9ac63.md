# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: mobile-layout.e2e.ts >> E2E-MOBILE-01: ヘッダー・下部タブ・ホーム >> 未ログインでヒーロー・特徴カードが1列になる（320px）
- Location: e2e/mobile-layout.e2e.ts:89:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('.hero')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('.hero')

```

```yaml
- banner:
  - link "こんだてめいと":
    - /url: /
    - heading "こんだてめいと" [level=1]
  - 'button "現在: システム設定に合わせる。クリックでライトモードに切り替え"'
  - button "Open user menu":
    - img "'s logo"
- main: 家庭の準備に失敗しました。API が起動しているか確認して、再読み込みしてください。
- navigation "メインナビゲーション":
  - link "献立":
    - /url: /menu
  - link "買い物":
    - /url: /shopping
  - link "レシピ":
    - /url: /recipes
  - link "Pantry":
    - /url: /pantry
  - link "ルール":
    - /url: /rules
- alert
```

# Test source

```ts
  1   | import { type Page, type Route, expect, test } from "@playwright/test";
  2   | 
  3   | const hasClerkConfig =
  4   |   Boolean(process.env.CLERK_PUBLISHABLE_KEY) &&
  5   |   Boolean(process.env.CLERK_SECRET_KEY);
  6   | 
  7   | test.skip(!hasClerkConfig, "Clerk E2E用の2つのキーが必要です");
  8   | 
  9   | const MOBILE_WIDTHS = [320, 375, 390, 430] as const;
  10  | const VIEWPORT_HEIGHT = 844;
  11  | 
  12  | async function prepareAuthenticatedPage(page: Page): Promise<void> {
  13  |   await page.addInitScript(() => {
  14  |     window.localStorage.setItem("kondate-mate.householdId", "e2e-household");
  15  |   });
  16  | }
  17  | 
  18  | function json(route: Route, body: unknown, status = 200): Promise<void> {
  19  |   return route.fulfill({
  20  |     status,
  21  |     contentType: "application/json",
  22  |     body: JSON.stringify(body),
  23  |   });
  24  | }
  25  | 
  26  | /**
  27  |  * ページ全体の横方向オーバーフローを検出する。
  28  |  * `.container` 等の一部要素だけに限定しない。
  29  |  */
  30  | async function expectNoHorizontalOverflow(page: Page): Promise<void> {
  31  |   const { scrollWidth, clientWidth } = await page.evaluate(() => ({
  32  |     scrollWidth: document.documentElement.scrollWidth,
  33  |     clientWidth: document.documentElement.clientWidth,
  34  |   }));
  35  |   expect(
  36  |     scrollWidth,
  37  |     "横方向オーバーフローが発生しています",
  38  |   ).toBeLessThanOrEqual(clientWidth);
  39  | }
  40  | 
  41  | async function forEachMobileWidth(
  42  |   page: Page,
  43  |   run: (width: number) => Promise<void>,
  44  | ): Promise<void> {
  45  |   for (const width of MOBILE_WIDTHS) {
  46  |     await page.setViewportSize({ width, height: VIEWPORT_HEIGHT });
  47  |     await run(width);
  48  |   }
  49  | }
  50  | 
  51  | test.describe("E2E-MOBILE-01: ヘッダー・下部タブ・ホーム", () => {
  52  |   test("ログイン済みでヘッダー・下部タブ・ナビカードが崩れない", async ({
  53  |     page,
  54  |   }) => {
  55  |     await prepareAuthenticatedPage(page);
  56  |     await page.route("http://localhost:3001/**", async (route) => {
  57  |       const path = new URL(route.request().url()).pathname;
  58  |       if (path === "/households/ensure") {
  59  |         await json(route, { householdId: "e2e-household", created: false });
  60  |         return;
  61  |       }
  62  |       await route.fulfill({ status: 404 });
  63  |     });
  64  | 
  65  |     await forEachMobileWidth(page, async () => {
  66  |       await page.goto("/");
  67  |       await page.getByText("おかえりなさい").waitFor();
  68  |       await expectNoHorizontalOverflow(page);
  69  |       await expect(
  70  |         page
  71  |           .getByRole("heading", { name: "こんだてめいと" })
  72  |           .or(page.locator(".brand h1"))
  73  |           .first(),
  74  |       ).toBeVisible();
  75  |       await expect(
  76  |         page.getByRole("link", { name: /今週の献立/ }),
  77  |       ).toBeVisible();
  78  | 
  79  |       const tabbar = page.locator(".tabbar");
  80  |       await expect(tabbar).toBeVisible();
  81  |       const tabItems = tabbar.locator(".tab-item");
  82  |       await expect(tabItems).toHaveCount(5);
  83  |       for (const item of await tabItems.all()) {
  84  |         await expect(item).toBeVisible();
  85  |       }
  86  |     });
  87  |   });
  88  | 
  89  |   test("未ログインでヒーロー・特徴カードが1列になる（320px）", async ({
  90  |     page,
  91  |   }) => {
  92  |     await page.setViewportSize({ width: 320, height: VIEWPORT_HEIGHT });
  93  |     await page.goto("/");
  94  |     await expectNoHorizontalOverflow(page);
> 95  |     await expect(page.locator(".hero")).toBeVisible();
      |                                         ^ Error: expect(locator).toBeVisible() failed
  96  |     const features = page.locator(".feature");
  97  |     await expect(features).toHaveCount(3);
  98  |     const firstBox = await features.nth(0).boundingBox();
  99  |     const secondBox = await features.nth(1).boundingBox();
  100 |     expect(firstBox).not.toBeNull();
  101 |     expect(secondBox).not.toBeNull();
  102 |     // 1列（縦積み）であれば2枚目のtopは1枚目のbottom以降になる。
  103 |     if (firstBox !== null && secondBox !== null) {
  104 |       expect(secondBox.y).toBeGreaterThanOrEqual(
  105 |         firstBox.y + firstBox.height - 1,
  106 |       );
  107 |     }
  108 |   });
  109 | });
  110 | 
  111 | test.describe("E2E-MOBILE-02: 週の献立", () => {
  112 |   test("操作群が下段に回り、カード幅と一致する", async ({ page }) => {
  113 |     await prepareAuthenticatedPage(page);
  114 |     const recipes = [
  115 |       {
  116 |         id: "recipe-1",
  117 |         title: "とても長い名前の豚肉と野菜のスタミナ炒め定食セット",
  118 |         category: "meat",
  119 |         ingredients: [],
  120 |         steps: [],
  121 |       },
  122 |     ];
  123 |     const menu = {
  124 |       weekStart: "2026-07-20",
  125 |       items: Array.from({ length: 7 }, (_, day) => ({
  126 |         day,
  127 |         recipeId: "recipe-1",
  128 |         status: "Planned",
  129 |       })),
  130 |     };
  131 | 
  132 |     await page.route("http://localhost:3001/**", async (route) => {
  133 |       const path = new URL(route.request().url()).pathname;
  134 |       if (path === "/recipes") return json(route, recipes);
  135 |       if (path.startsWith("/weekly-menus/")) return json(route, menu);
  136 |       await route.fulfill({ status: 404 });
  137 |     });
  138 | 
  139 |     await forEachMobileWidth(page, async (width) => {
  140 |       await page.goto("/menu");
  141 |       await page.locator(".day").first().waitFor();
  142 |       await expectNoHorizontalOverflow(page);
  143 | 
  144 |       const day = page.locator(".day").first();
  145 |       const recipeName = day.locator(".recipe-name");
  146 |       const ops = day.locator(".ops");
  147 |       const dayBox = await day.boundingBox();
  148 |       const recipeBox = await recipeName.boundingBox();
  149 |       const opsBox = await ops.boundingBox();
  150 |       expect(dayBox).not.toBeNull();
  151 |       expect(recipeBox).not.toBeNull();
  152 |       expect(opsBox).not.toBeNull();
  153 |       if (dayBox !== null && recipeBox !== null && opsBox !== null) {
  154 |         // 操作群はレシピ情報より下にある。
  155 |         expect(opsBox.y).toBeGreaterThan(recipeBox.y);
  156 |         // カード内側の利用可能幅（.day のコンテンツ幅）と2px以内で一致する。
  157 |         const dayContentWidth = dayBox.width - 28; // padding: 10px 14px
  158 |         expect(
  159 |           Math.abs(opsBox.width - dayContentWidth),
  160 |           `width=${width}: ops幅がカード内側幅と一致しません`,
  161 |         ).toBeLessThanOrEqual(2);
  162 |       }
  163 |       const justifyContent = await ops.evaluate(
  164 |         (el) => getComputedStyle(el).justifyContent,
  165 |       );
  166 |       expect(justifyContent).toBe("flex-start");
  167 |     });
  168 |   });
  169 | });
  170 | 
  171 | test.describe("E2E-MOBILE-03: 買い物リスト", () => {
  172 |   test("追加フォームが320pxで縦積みになる", async ({ page }) => {
  173 |     await prepareAuthenticatedPage(page);
  174 |     const list = {
  175 |       weekStart: "2026-07-20",
  176 |       items: [
  177 |         {
  178 |           ingredientId: "very-long-name",
  179 |           name: "とても長い名前の食材の名前が続く長文の食材名テスト",
  180 |           amount: "たくさん",
  181 |           purchased: false,
  182 |         },
  183 |       ],
  184 |     };
  185 |     await page.route("http://localhost:3001/**", async (route) => {
  186 |       if (route.request().method() === "GET") return json(route, list);
  187 |       await route.fulfill({ status: 404 });
  188 |     });
  189 | 
  190 |     await page.setViewportSize({ width: 320, height: VIEWPORT_HEIGHT });
  191 |     await page.goto("/shopping");
  192 |     await page.locator(".shopping-add-row").waitFor();
  193 |     await expectNoHorizontalOverflow(page);
  194 | 
  195 |     const nameInput = page.getByLabel("追加する食材名");
```