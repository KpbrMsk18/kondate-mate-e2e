# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: mobile-layout.e2e.ts >> E2E-MOBILE-04: レシピ一覧・フォーム・詳細 >> 一覧・登録・詳細が320〜430pxで崩れない
- Location: e2e/mobile-layout.e2e.ts:231:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.waitFor: Test timeout of 30000ms exceeded.
Call log:
  - waiting for locator('.recipe-row').first() to be visible

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - banner [ref=e2]:
    - link "こんだてめいと" [ref=e3] [cursor=pointer]:
      - /url: /
      - img [ref=e5]
      - heading "こんだてめいと" [level=1] [ref=e7]
    - 'button "現在: システム設定に合わせる。クリックでライトモードに切り替え" [ref=e9] [cursor=pointer]':
      - img [ref=e10]
  - main [ref=e12]:
    - generic [ref=e13]:
      - generic [ref=e14]:
        - heading "レシピ" [level=2] [ref=e15]
        - link "レシピを登録" [ref=e16] [cursor=pointer]:
          - /url: /recipes/new
          - img [ref=e17]
          - text: レシピを登録
      - generic [ref=e19]: 読み込んでいます…
  - navigation "メインナビゲーション" [ref=e21]:
    - link "献立" [ref=e22] [cursor=pointer]:
      - /url: /menu
      - img [ref=e23]
      - text: 献立
    - link "買い物" [ref=e25] [cursor=pointer]:
      - /url: /shopping
      - img [ref=e26]
      - text: 買い物
    - link "レシピ" [ref=e28] [cursor=pointer]:
      - /url: /recipes
      - img [ref=e29]
      - text: レシピ
    - link "Pantry" [ref=e31] [cursor=pointer]:
      - /url: /pantry
      - img [ref=e32]
      - text: Pantry
    - link "ルール" [ref=e34] [cursor=pointer]:
      - /url: /rules
      - img [ref=e35]
      - text: ルール
  - button "Open Next.js Dev Tools" [ref=e42] [cursor=pointer]:
    - img [ref=e43]
  - alert [ref=e46]
```

# Test source

```ts
  155 |         expect(opsBox.y).toBeGreaterThan(recipeBox.y);
  156 |         // カード内側の利用可能幅（.day のコンテンツ幅）と2px以内で一致する。
  157 |         const dayContentWidth = dayBox.width - 28; // padding: 10px 14px
  158 |         expect(
  159 |           Math.abs(opsBox.width - dayContentWidth),
  160 |           `width=${width}: ops幅がカード内側幅と一致しません`,
  161 |         ).toBeLessThanOrEqual(2);
  162 |       }
  163 |       const justifyContent = await ops.evaluate(
  164 |         (el) => getComputedStyle(el).justifyContent,
  165 |       );
  166 |       expect(justifyContent).toBe("flex-start");
  167 |     });
  168 |   });
  169 | });
  170 | 
  171 | test.describe("E2E-MOBILE-03: 買い物リスト", () => {
  172 |   test("追加フォームが320pxで縦積みになる", async ({ page }) => {
  173 |     await prepareAuthenticatedPage(page);
  174 |     const list = {
  175 |       weekStart: "2026-07-20",
  176 |       items: [
  177 |         {
  178 |           ingredientId: "very-long-name",
  179 |           name: "とても長い名前の食材の名前が続く長文の食材名テスト",
  180 |           amount: "たくさん",
  181 |           purchased: false,
  182 |         },
  183 |       ],
  184 |     };
  185 |     await page.route("http://localhost:3001/**", async (route) => {
  186 |       if (route.request().method() === "GET") return json(route, list);
  187 |       await route.fulfill({ status: 404 });
  188 |     });
  189 | 
  190 |     await page.setViewportSize({ width: 320, height: VIEWPORT_HEIGHT });
  191 |     await page.goto("/shopping");
  192 |     await page.locator(".shopping-add-row").waitFor();
  193 |     await expectNoHorizontalOverflow(page);
  194 | 
  195 |     const nameInput = page.getByLabel("追加する食材名");
  196 |     const amountInput = page.getByLabel("追加する食材の分量");
  197 |     const addButton = page.getByRole("button", { name: "追加", exact: true });
  198 |     const row = page.locator(".shopping-add-row");
  199 |     const rowBox = await row.boundingBox();
  200 |     const nameBox = await nameInput.boundingBox();
  201 |     const amountBox = await amountInput.boundingBox();
  202 |     const addBox = await addButton.boundingBox();
  203 |     expect(rowBox).not.toBeNull();
  204 |     expect(nameBox).not.toBeNull();
  205 |     expect(amountBox).not.toBeNull();
  206 |     expect(addBox).not.toBeNull();
  207 |     if (
  208 |       rowBox !== null &&
  209 |       nameBox !== null &&
  210 |       amountBox !== null &&
  211 |       addBox !== null
  212 |     ) {
  213 |       // 縦積み: 分量は食材名より下、追加ボタンは分量より下。
  214 |       expect(amountBox.y).toBeGreaterThan(nameBox.y);
  215 |       expect(addBox.y).toBeGreaterThan(amountBox.y);
  216 |       for (const box of [nameBox, amountBox, addBox]) {
  217 |         expect(Math.abs(box.width - rowBox.width)).toBeLessThanOrEqual(2);
  218 |       }
  219 |     }
  220 | 
  221 |     // 長い食材名でも操作ボタンが画面外へ押し出されない。
  222 |     const item = page.locator(".shopping-item").first();
  223 |     await expect(item).toBeVisible();
  224 |     await expectNoHorizontalOverflow(page);
  225 |     const actions = item.locator(".shopping-item-actions");
  226 |     await expect(actions.getByRole("button", { name: "編集" })).toBeVisible();
  227 |   });
  228 | });
  229 | 
  230 | test.describe("E2E-MOBILE-04: レシピ一覧・フォーム・詳細", () => {
  231 |   test("一覧・登録・詳細が320〜430pxで崩れない", async ({ page }) => {
  232 |     await prepareAuthenticatedPage(page);
  233 |     const recipes = [
  234 |       {
  235 |         id: "recipe-1",
  236 |         title: "とても長い名前のレシピタイトルが続くケース",
  237 |         category: "fish",
  238 |         ingredients: [{ ingredientId: "i1", name: "鮭", amount: "2切れ" }],
  239 |         steps: ["下ごしらえをする", "強火で焼き色をつけてから弱火で仕上げる"],
  240 |       },
  241 |     ];
  242 |     await page.route("http://localhost:3001/**", async (route) => {
  243 |       const request = route.request();
  244 |       const path = new URL(request.url()).pathname;
  245 |       if (path === "/recipes" && request.method() === "GET") {
  246 |         return json(route, recipes);
  247 |       }
  248 |       const recipe = recipes.find((item) => path === `/recipes/${item.id}`);
  249 |       if (recipe !== undefined) return json(route, recipe);
  250 |       await route.fulfill({ status: 404 });
  251 |     });
  252 | 
  253 |     await forEachMobileWidth(page, async () => {
  254 |       await page.goto("/recipes");
> 255 |       await page.locator(".recipe-row").first().waitFor();
      |                                                 ^ Error: locator.waitFor: Test timeout of 30000ms exceeded.
  256 |       await expectNoHorizontalOverflow(page);
  257 |       await expect(
  258 |         page.getByRole("button", { name: /削除/ }).first(),
  259 |       ).toBeVisible();
  260 | 
  261 |       await page.goto("/recipes/new");
  262 |       await page.getByLabel("レシピ名").waitFor();
  263 |       await expectNoHorizontalOverflow(page);
  264 |       const ingredientRow = page.locator(".ingredient-row").first();
  265 |       await expect(
  266 |         ingredientRow.getByRole("button", { name: "表記候補" }),
  267 |       ).toBeVisible();
  268 |       await expect(
  269 |         ingredientRow.getByRole("button", { name: "削除" }),
  270 |       ).toBeVisible();
  271 |       const stepRow = page.locator(".step-row").first();
  272 |       await expect(stepRow.locator(".step-number")).toBeVisible();
  273 | 
  274 |       await page.goto("/recipes/recipe-1");
  275 |       await expect(
  276 |         page.getByRole("heading", { name: recipes[0]?.title }),
  277 |       ).toBeVisible();
  278 |       await expectNoHorizontalOverflow(page);
  279 |     });
  280 |   });
  281 | });
  282 | 
  283 | test.describe("E2E-MOBILE-05: Pantry", () => {
  284 |   test("320pxで食材名が上段、保存場所と削除が下段になる", async ({ page }) => {
  285 |     await prepareAuthenticatedPage(page);
  286 |     const items = [
  287 |       {
  288 |         ingredientId: "egg",
  289 |         name: "とても長い名前の食材テストデータ",
  290 |         storageLocation: "refrigerated",
  291 |       },
  292 |     ];
  293 |     await page.route("http://localhost:3001/**", async (route) => {
  294 |       if (route.request().method() === "GET") return json(route, items);
  295 |       await route.fulfill({ status: 404 });
  296 |     });
  297 | 
  298 |     await page.setViewportSize({ width: 320, height: VIEWPORT_HEIGHT });
  299 |     await page.goto("/pantry");
  300 |     await page.locator(".pantry-item-row").first().waitFor();
  301 |     await expectNoHorizontalOverflow(page);
  302 | 
  303 |     const row = page.locator(".pantry-item-row").first();
  304 |     const nameEl = row.locator(".pantry-item-name");
  305 |     const nameBox = await nameEl.boundingBox();
  306 |     const rowBox = await row.boundingBox();
  307 |     expect(nameBox).not.toBeNull();
  308 |     expect(rowBox).not.toBeNull();
  309 |     if (nameBox !== null && rowBox !== null) {
  310 |       // 名称領域が極端に狭くならない（行幅の半分以上を確保）。
  311 |       expect(nameBox.width).toBeGreaterThan(rowBox.width * 0.4);
  312 |     }
  313 |   });
  314 | });
  315 | 
  316 | test.describe("E2E-MOBILE-06: 家庭のルール", () => {
  317 |   test("320pxで曜日・レシピ・登録ボタンが縦積みになる", async ({ page }) => {
  318 |     await prepareAuthenticatedPage(page);
  319 |     await page.route("http://localhost:3001/**", async (route) => {
  320 |       const path = new URL(route.request().url()).pathname;
  321 |       if (path === "/household/fixed-menu-rules") return json(route, []);
  322 |       if (path === "/recipes") {
  323 |         return json(route, [
  324 |           { id: "recipe-1", title: "カレーライス", category: "meat" },
  325 |         ]);
  326 |       }
  327 |       if (path === "/household/menu-preference") {
  328 |         return json(route, { meatRatio: 50 });
  329 |       }
  330 |       await route.fulfill({ status: 404 });
  331 |     });
  332 | 
  333 |     await page.setViewportSize({ width: 320, height: VIEWPORT_HEIGHT });
  334 |     await page.goto("/rules");
  335 |     await page.locator(".add-row").waitFor();
  336 |     await expectNoHorizontalOverflow(page);
  337 | 
  338 |     const addRow = page.locator(".add-row");
  339 |     const daySelect = page.getByLabel("曜日");
  340 |     const recipeSelect = page.getByLabel("レシピ");
  341 |     const registerButton = page.getByRole("button", { name: "ルールを登録" });
  342 |     const rowBox = await addRow.boundingBox();
  343 |     const dayBox = await daySelect.boundingBox();
  344 |     const recipeBox = await recipeSelect.boundingBox();
  345 |     const buttonBox = await registerButton.boundingBox();
  346 |     expect(rowBox).not.toBeNull();
  347 |     expect(dayBox).not.toBeNull();
  348 |     expect(recipeBox).not.toBeNull();
  349 |     expect(buttonBox).not.toBeNull();
  350 |     if (
  351 |       rowBox !== null &&
  352 |       dayBox !== null &&
  353 |       recipeBox !== null &&
  354 |       buttonBox !== null
  355 |     ) {
```